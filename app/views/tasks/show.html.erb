<h1> <%= @task.title %></h1>
<p><%= @task.details %></p>


<%= check_box("task", @task.id, {checked: :completed}) %>

<%= link_to 'Back to list', tasks_path %>

<%= button_to "Edit this task", tasks_edit_path, method: :get %>
